#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main ()
{
    char filename[99];

    printf("please input filename:");
    scanf("%99s", filename);

if {
    (strcmp (filename[99] == "HelloWorld") )
    fopen("HelloWorld.txt","r");
}
    if { (strcmp(filename[99] = NULL) )
    printf("failure . that's on you buddy");
    return 0;
}
}